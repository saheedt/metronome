import { describe, it, expect } from "vitest";
import { BitlinkStore } from "../src/store.js";

describe("BitlinkStore", () => {
  it.todo("stores and retrieves a bitlink");
  it.todo("returns undefined for unknown bitlinks");
  it.todo("handles branded domains");
  it.todo("overwrites duplicate bitlink with warning");
  it.todo("reports correct size");
});
