import { describe, it, expect } from "vitest";
import { processClicks } from "../src/processor.js";

describe("processClicks", () => {
  it.todo("counts clicks per long URL for a given year");
  it.todo("returns zero counts for URLs with no clicks");
  it.todo("skips unmatched bitlinks silently");
  it.todo("produces correct output for real data");
});
