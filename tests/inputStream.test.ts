import { describe, it, expect } from "vitest";
import { createInputStream } from "../src/inputStream.js";

describe("createInputStream", () => {
  it.todo("parses CSV with correct fields");
  it.todo("parses JSON with correct fields");
  it.todo("throws on missing required fields");
  it.todo("throws on unsupported file extension");
  it.todo("throws on file not found");
  it.todo("handles empty files gracefully");
});
