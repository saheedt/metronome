/**
 * Normalize a raw bitlink URL to canonical form: "domain/hash".
 * Strips protocol, lowercases domain, preserves hash case.
 */
export function normalizeBitlink(_raw: string): string {
  // TODO: implement
  return "";
}

/**
 * Build a normalized bitlink key from separate domain and hash components.
 */
export function buildBitlink(_domain: string, _hash: string): string {
  // TODO: implement
  return "";
}
